import { render } from "@testing-library/react";
import { nanoid } from "nanoid";
import React, { useState } from "react";
import { useResolvedPath } from "react-router";
import "./App.css";

function App() {
// const [obj,setObj]=useState({prop1:'1',prop2:'2',prop3:'3'})
// console.log(obj);


  return (
    <>
{/* <button onClick={()=>setObj({...obj,...{prop1:'!'}})}>click me1</button>
<button onClick={()=>setObj({...obj,...{prop2:'!!'}})}>click me2</button>
<button onClick={()=>setObj({...obj,...{prop3:'!!!'}})}>click me3</button>
Сделайте 3 кнопки. Пусть первая кнопка изменяет значение свойства prop1, вторая - prop2, а третья - prop3. */}

    </>
  );
}
export default App;
