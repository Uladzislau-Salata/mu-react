import { render } from "@testing-library/react";
import { nanoid } from "nanoid";
import React, { useState } from "react";
import "./App.css";

function App() {

//   const [arrs,setArrs] =useState(['a', 'b', 'c', 'd', 'e']);
// function dell(s) {
//   setArrs([...arrs.slice(0,s),...arrs.slice(s+1)]);
// }
// const result=arrs.map((elem,index)=>{
// return <li key={index} >{elem} <button onClick={()=>dell(index)}>click me</button> </li>
// })


  return (
    <>
    {/* <ul>
      {result}
    </ul>
Выведите элементы этого массива в виде списка ul. Сделайте так, чтобы в конце каждой li стояла кнопка для ее удаления. */}


    </>
  );
}
export default App;
